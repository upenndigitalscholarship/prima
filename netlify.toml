[build]
publish = "site/"
command = """
sudo -E apt install software-properties-common -y &&
sudo -E add-apt-repository ppa:deadsnakes/ppa &&
sudo -E apt install python3.10 &&
sudo -E apt install python3.10-venv &&
python3.10 -m venv venv &&
source venv/bin/activate &&
curl -sSL https://install.python-poetry.org | python3.10 - &&
poetry install &&
prima build
"""
