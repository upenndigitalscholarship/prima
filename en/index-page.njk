---
title: Index
---

<div class="container">
    <div>
    <h1>Index</h1>
    <p>Here you can find a list of all the lessons available on the site, organized by topic.</p>

 {% for cat in collections.lessons | makeIndex %}
      
        {% for key, items in cat %}
        <h2>{{ key }}</h2>
        <ul>
            {% for subkey, subitems in items %}
            <li><a href="{{ "/en/lessons/?q=" + subkey + "&filter=" + key }}">{{ subkey }} </a></li>
                <ul>
                {% for item in subitems %}
                <li><a href="{{ "/en/lessons/?q=" + item + "&filter=" + key }}">{{ item }}</a></li>
                {% endfor %}
                </ul>
            {% endfor %}
        </ul>  
        {% endfor %}
   {% endfor %}   
</div>
</div>