{% extends "base.njk" %}

{% block content %}
    <a href="{{ "/browse-media/" | locale_url }}">
        < back</a>

           <div class="container"> 
               <div>
                    <h1>{{ lesson }}</h1>
                </div>
            
                
                <div>
                    {% if video_url %}
                        <video controls>
                            <source src="{{ video_url }}" type="video/mp4">
                        </video>
                    {% else %}
                        <img height="50%" src="{{ thumbnail }}"/>
                    {% endif %}
                </div>
            </div>

           
                
        {% endblock %}